// ============================================================
// events/index.ts — barrel re-export
// ============================================================

export { registerCoreEvents } from './loader';
export { triggerExploreEvent, triggerDailyEvent } from './triggers';
